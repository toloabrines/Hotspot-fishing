name: Build iOS and upload to TestFlight (legacy disabled)

on:
  workflow_dispatch:

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow disabled
        run: |
          echo "Este workflow antiguo está desactivado para evitar duplicados y errores de firma."
          echo "El workflow mantenido es: .github/workflows/ios-testflight.yml"
